import { SimpleNavbar } from '@/app/components/SimpleNavbar'

export default function Header() {
  // Use the central / canonical app Navbar for the pages router to keep the
  // navigation consistent across app and pages routes.
  return <SimpleNavbar />
}
