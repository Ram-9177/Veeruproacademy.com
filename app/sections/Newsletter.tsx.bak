'use client'

import { useState } from 'react'
import { motion } from 'framer-motion'
import { Section } from '../components/Section'
import { SectionHeader } from '../components/SectionHeader'
import { Input } from '../components/Input'
import { Button } from '../components/Button'

export function Newsletter() {
  const [email, setEmail] = useState('')

  return (
    <Section className="py-10 md:py-16">
      <motion.div
        initial={{ opacity: 0, y: 20 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true, margin: '-50px' }}
        transition={{ duration: 0.5 }}
        className="rounded-2xl border border-indigo-100 bg-gradient-to-br from-indigo-600 via-indigo-500 to-amber-400 px-4 py-8 text-white shadow-xl shadow-indigo-500/30 dark:border-indigo-500/40 dark:shadow-indigo-900/40 md:rounded-3xl md:px-8 md:py-12"
      >
        <div className="flex flex-col gap-6 md:gap-8">
          <SectionHeader
            title="Weekly drops"
            description="Projects, prompts, and UI assets straight to your inbox."
            align="center"
            tone="light"
            subtitle="No spam—just actionable ideas"
          />

          <motion.form
            initial={{ opacity: 0, y: 10 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true, margin: '-50px' }}
            transition={{ duration: 0.5, delay: 0.1 }}
            className="mx-auto w-full max-w-md space-y-3 rounded-xl bg-white/10 p-4 shadow-lg shadow-indigo-900/20 backdrop-blur md:p-6"
            onSubmit={e => {
              e.preventDefault()
            }}
          >
            <Input
              placeholder="you@example.com"
              type="email"
              value={email}
              onChange={e => setEmail(e.target.value)}
              required
              className="border-white/30 bg-white/15 text-white text-sm placeholder:text-white/70 focus:border-white/50 focus:ring-white/40 md:text-base"
            />
            <Button type="submit" size="lg" className="w-full text-sm md:text-base">
              Subscribe Now
            </Button>
            <p className="text-center text-xs font-medium text-white/80">
              Unsubscribe anytime · No spam
            </p>
          </motion.form>
        </div>
      </motion.div>
    </Section>
  )
}
