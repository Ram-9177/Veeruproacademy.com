'use client'

import { useSearchParams } from 'next/navigation'
import { AlertCircle, ShieldCheck, User, Lock, XCircle } from 'lucide-react'
import Link from 'next/link'

export default function LoginPage() {
  const searchParams = useSearchParams()
  const message = searchParams?.get('message')

  return (
    <div className="min-h-screen" style={{ backgroundColor: '#0a0a0a' }}>
      {/* Beautiful Hero Header */}
      <div className="bg-gradient-to-br from-red-600 to-orange-600 text-white">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
          <div className="text-center max-w-4xl mx-auto">
            {/* Badge */}
            <div className="inline-flex items-center gap-2 px-4 py-2 bg-white/20 rounded-full text-sm font-semibold mb-6">
              <XCircle className="w-4 h-4" />
              Direct Login Disabled
            </div>

            {/* Title */}
            <h1 className="text-5xl md:text-6xl font-bold mb-6">
              Direct Login Disabled
            </h1>
            <p className="text-2xl md:text-3xl font-semibold text-white/90 mb-8">
              This Feature is Temporarily Unavailable
            </p>

            {/* Description */}
            <p className="text-xl text-white/80 mb-12 leading-relaxed">
              Direct email/password login has been disabled. Please contact the administrator to enable OAuth authentication (Google, GitHub, etc.) or request access.
            </p>
          </div>
        </div>
      </div>

      {/* Main Content */}
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
        <div className="max-w-2xl mx-auto">
          
          {/* Notice Card */}
          <div className="bg-white/5 rounded-3xl p-8 border border-white/10">
            <div className="text-center mb-8">
              <div className="w-16 h-16 bg-gradient-to-r from-red-500 to-orange-500 rounded-2xl flex items-center justify-center mx-auto mb-4">
                <Lock className="w-8 h-8 text-white" />
              </div>
              <h2 className="text-2xl font-bold mb-2" style={{ color: '#ffffff' }}>Authentication Required</h2>
              <p className="text-sm" style={{ color: '#a1a1aa' }}>
                Direct credentials login is currently disabled
              </p>
            </div>

            {message && (
              <div className="mb-6 p-4 rounded-2xl border bg-green-500/20 border-green-500/30 flex items-start gap-3">
                <div className="h-5 w-5 flex-shrink-0 mt-0.5 text-green-400">✓</div>
                <p className="text-sm leading-6 text-green-300">{message}</p>
              </div>
            )}

            {/* Information Box */}
            <div className="mb-6 p-6 rounded-2xl border bg-orange-500/10 border-orange-500/30">
              <div className="flex items-start gap-3">
                <AlertCircle className="h-5 w-5 flex-shrink-0 mt-0.5 text-orange-400" />
                <div>
                  <h3 className="text-sm font-semibold text-orange-300 mb-2">Why is this disabled?</h3>
                  <p className="text-sm leading-6 text-orange-200/80">
                    For security purposes, direct email/password authentication has been temporarily disabled. 
                    The platform is transitioning to OAuth-based authentication for enhanced security.
                  </p>
                </div>
              </div>
            </div>

            {/* Alternative Options */}
            <div className="space-y-4">
              <h3 className="text-lg font-semibold text-center mb-4" style={{ color: '#ffffff' }}>
                Alternative Options
              </h3>

              {/* OAuth Coming Soon */}
              <div className="p-4 rounded-2xl bg-white/5 border border-white/10">
                <div className="flex items-center gap-3 mb-2">
                  <ShieldCheck className="h-5 w-5 text-blue-400" />
                  <h4 className="font-semibold" style={{ color: '#ffffff' }}>OAuth Authentication</h4>
                </div>
                <p className="text-sm ml-8" style={{ color: '#a1a1aa' }}>
                  Google, GitHub, and other OAuth providers will be available soon. 
                  Contact your administrator to enable these options.
                </p>
              </div>

              {/* Contact Admin */}
              <div className="p-4 rounded-2xl bg-white/5 border border-white/10">
                <div className="flex items-center gap-3 mb-2">
                  <User className="h-5 w-5 text-emerald-400" />
                  <h4 className="font-semibold" style={{ color: '#ffffff' }}>Need Access?</h4>
                </div>
                <p className="text-sm ml-8" style={{ color: '#a1a1aa' }}>
                  If you need access to the platform, please contact your administrator or system owner 
                  to discuss authentication options.
                </p>
              </div>
            </div>

            <div className="mt-8 text-center space-y-4">
              <div className="pt-4 border-t border-white/10">
                <p className="text-sm" style={{ color: '#a1a1aa' }}>
                  Are you an administrator?{' '}
                  <Link href="/admin/login" className="font-semibold hover:text-emerald-300 transition-colors" style={{ color: '#10b981' }}>
                    Admin Login
                  </Link>
                </p>
                <p className="text-sm mt-2" style={{ color: '#a1a1aa' }}>
                  Go back to{' '}
                  <Link href="/" className="font-semibold hover:text-blue-300 transition-colors" style={{ color: '#3b82f6' }}>
                    Homepage
                  </Link>
                </p>
              </div>
            </div>
          </div>

          {/* Technical Note */}
          <div className="mt-8 p-4 rounded-2xl bg-white/5 border border-white/10">
            <p className="text-xs text-center" style={{ color: '#71717a' }}>
              <strong>Technical Note:</strong> Direct credentials authentication is disabled in lib/auth.ts. 
              To re-enable, uncomment the CredentialsProvider and update the providers array.
            </p>
          </div>
        </div>
      </div>
    </div>
  )
}